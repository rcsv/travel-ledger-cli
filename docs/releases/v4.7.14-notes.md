# v4.7.14 — Public examples guard CI isolation hotfix

## Summary

**v4.7.14** fixes Rust CI flakiness in `public_examples_validation_guard` — `duplicate column name: day_id` from parallel tests sharing the default DB during `trip validate-export`.

Spec: [v4.7.14-public-examples-guard-ci-isolation-hotfix.md](../specifications/v4.7.14-public-examples-guard-ci-isolation-hotfix.md)

## What changed

- **Fix:** `trip validate-export` and `trip diff` dispatch before DB open (file-only, like `proposal validate`)
- **Fix:** guard tests use isolated temp working directories per test
- **Not changed:** validation rules, schema v8, public examples, DB migrations

## Compatibility

- **JSON golden:** unchanged
- **Markdown golden:** colophon version only (`4.7.14`)
- **Behavior:** `trip validate-export` / `trip diff` no longer open SQLite (intended — no DB needed)

## Next milestone

**P-5 candidate** — materialize / apply planning spec — consultation
