# v4.6.29 — Itinerary show aggregate migration plan

## Summary

**v4.6.29** is a **documentation-only migration plan** after [v4.6.28](../specifications/v4.6.28-itinerary-show-aggregate-boundary-review.md). It defines phased migration for itinerary show aggregate ownership without implementation.

Plan: [v4.6.29-itinerary-show-aggregate-migration-plan.md](../specifications/v4.6.29-itinerary-show-aggregate-migration-plan.md)

## Headline decisions

- **Short term:** Option A — keep handler human-only Reservations fetch
- **Do not** rush Option B (reservations-only in `ItineraryShowServiceResult`)
- **Mid term:** Option D (GUI/Tauri aggregate DTO) + Option C (separate aggregate detail service)
- **GUI v1:** compose existing read-only services (`itinerary_show` + `reservation_list` + …)
- **Gates:** `itinerary show --json` stays `ItineraryItem` only; human output unchanged

## Phases (summary)

| Phase | Scope |
|---|---|
| 0 | v4.6.29 plan (this release) |
| 1 | CLI unchanged |
| 2 | GUI compose individual services |
| 3 | Option C/D when UX requires aggregate |
| 4 | Optional CLI human internal refactor |

## Non-goals

- Code, golden, JSON/human changes, aggregate DTO implementation

## Next milestone

**v4.6.30 — expense write path boundary review** (recommended)

Alternatives: SQLite migration runner; itinerary aggregate Phase 2+ when GUI starts.
