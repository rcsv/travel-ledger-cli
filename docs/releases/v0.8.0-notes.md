# v0.8.0 - Trip Advisor

## Added

- Added `trip advisor <trip_id>` command.
- Added rule-based travel planning advice based on doctor issues.
- Added structured `DoctorIssueCode` / `DoctorIssue`.
- Added `analyze_trip_issues()` for reusable issue analysis.
- Added `src/advisor.rs`.
- Added advisor sample outputs under `samples/advisor/`.

## Changed

- Updated README with Advisor usage and project structure.
- Updated Cargo.toml version to `0.8.0`.

## Quality

- `cargo fmt` passed.
- `cargo clippy -- -D warnings` passed.
- `cargo test` passed.
- `cargo build` passed.

## Notes

This release evolves Caglla.Travel CLI from detecting travel planning issues to suggesting concrete improvements.
