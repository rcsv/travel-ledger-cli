# v4.7.34 — P-6k reorder_itinerary planning (documentation-only)

## Summary

**v4.7.34** adds a **documentation-only planning spec** for `reorder_itinerary` as a structural apply operation. The initial recommended scope is **same-day reorder only**, preserving **Sequence-first ordering** with `sort_order` as the source of truth and treating `start_time` as a label (not the primary reorder target).

Spec: [v4.7.34-p6k-reorder-itinerary-planning.md](../specifications/v4.7.34-p6k-reorder-itinerary-planning.md)

## What changed

- **New:** planning spec for `reorder_itinerary` (same-day reorder vs cross-day move split, baseline/conflict policy, preview shape, safety boundary, adoption path)
- **Updated:** current-work and documentation indexes
- **Not changed:** Rust code, tests, fixtures, schema v8, DB migration, runtime behavior

## Compatibility

- **JSON golden:** unchanged
- **Markdown golden:** colophon version only (`4.7.34`)
- **schema v8:** unchanged
- **DB migration:** none

## Next milestone

**v4.7.35 (P-6k)** — `reorder_itinerary` dry-run (same-day)

