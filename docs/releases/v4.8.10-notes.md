# v4.8.10 — Fragment apply structured errors post-release review

## Summary

**v4.8.10** is a **documentation-only** formal release: post-release **series review** of Fragment apply structured errors (v4.8.4 → v4.8.9).

No Rust code, tests, fixtures, CLI behavior, JSON serialization, or golden changes beyond Markdown colophon version sync. Cargo version bumped to `4.8.10`.

Review spec: [v4.8.10-fragment-apply-structured-errors-post-release-review.md](../specifications/v4.8.10-fragment-apply-structured-errors-post-release-review.md)

## Review conclusion

- **P0 / P1: none** — v4.8.4 → v4.8.9 series matches implementation and tests
- **Series complete:** planning → internal model → JSON exposure → public contract → limited wiring → delete_estimate confirm follow-up
- **`schema_version: 2` maintained** — additive `structured_errors[]` only
- **Legacy `errors[]` preserved** — backward-compatible channel
- **Client branching:** stable `code` is authoritative; `message` is unstable
- **Limited exposure confirmed** — unwired paths return `errors[]` only by design

## Wiring summary (v4.8.10)

- **15 registry codes** — 14 primary wired paths + 1 fallback (`APPLY_TRANSACTION_FAILED`)
- **delete_estimate confirm** — preview mismatch, TOCTOU, baseline, scoped write wired (v4.8.8–9)
- **Still deferred:** other intents, fragment/export validation, explicit fallback classification

## Provisional / deferred (P2)

- confirm transaction expansion to other intents (v4.8.11+)
- `verify_estimate_deleted` failure path
- `field_path` grammar formalization
- fragment / export validation wiring

## Not in scope

- Implementation changes
- `schema_version` bump
- JSON report shape changes

## Next candidate

```text
v4.8.11 — confirm transaction expansion or cross-cutting follow-up (optional)
Currency ISO validation (Issue #66)
Venue model (defer)
```
