# v4.6.16 — Read-only service boundary follow-up review

## Summary

**v4.6.16** is a **documentation-only follow-up review** after v4.6.12〜v4.6.15 list service boundaries (note, reservation, expense, checklist). It re-inventories all **11 service modules** from v4.6.4〜v4.6.15, confirms CLI/service/repository boundaries, assesses GUI read-only coverage, and recommends the next implementation step.

Review: [v4.6.16-read-only-service-boundary-follow-up-review.md](../specifications/v4.6.16-read-only-service-boundary-follow-up-review.md)

## Headline findings

- **11 service modules** — trip/day/itinerary core + note/reservation/expense/checklist list
- **No terminal I/O in services** — `println!` / `eprintln!` absent from `src/services/`
- **JSON wrappers stay in CLI** — NoteListJson, ReservationListJson, ExpenseListJson, DayListJson, etc.
- **GUI read-only list path largely complete** — show commands remain the main gap
- **Recommended next** — **v4.6.17 `checklist show` service boundary**

## Since v4.6.11

| Added | Version |
|---|---|
| note list | v4.6.12 |
| reservation list | v4.6.13 |
| expense list | v4.6.14 |
| checklist list | v4.6.15 |

v4.6.11 recommendation (note list) is **done**. Follow-up review confirms pattern consistency before show commands.

## Non-goals

- New service boundary implementation
- Rust functional changes
- CLI / JSON output changes
- DB schema / migration changes

## Next milestone

**v4.6.17 — checklist show service boundary** (recommended)
