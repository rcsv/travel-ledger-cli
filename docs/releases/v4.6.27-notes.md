# v4.6.27 — Expense output DTO migration follow-up review

## Summary

**v4.6.27** is a **documentation-only follow-up review** after [v4.6.26](../specifications/v4.6.26-expense-output-dto-migration-phase-2-3.md). It confirms read-only expense list/show context ownership moved to services, inventories retained adapters, assesses Phase 4 partial completion, and recommends keeping adapters until write-path work.

Review: [v4.6.27-expense-output-dto-migration-follow-up-review.md](../specifications/v4.6.27-expense-output-dto-migration-follow-up-review.md)

## Headline findings

- **Read-only path complete** — services return `ExpenseEnrichedPart`; CLI uses conn-free mapper/display
- **`ExpenseJson` stays outside service** — CLI wire DTO via `enriched_expense_to_json`
- **Adapters retained by design:**
  - `expense_to_json` — thin wrapper (`enrich` + mapper); tests only in production binary
  - `print_expense_detail` — **write path** (add/update after success)
  - `print_expense_list` — no `main.rs` callers; compatibility adapter
- **Phase 4 partially done** — enrichment moved; remaining cleanup tied to write path
- **Keep adapters** until write command service work
- **JSON / human gates** — unchanged at v4.6.26; maintain for future changes

## Non-goals

- Adapter removal or implementation changes
- Write command changes

## Next milestone

**v4.6.28 — itinerary show aggregate boundary review** (recommended)

Alternatives: expense write path boundary review; SQLite migration runner; v4.7.0 planning.
