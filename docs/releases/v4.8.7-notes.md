# v4.8.7 — Fragment apply structured errors public contract review / hardening

## Summary

**v4.8.7** is a **documentation-only** formal release: post-release review and **public contract hardening** for Fragment apply structured errors (v4.8.4 → v4.8.5 → v4.8.6).

No Rust code, tests, fixtures, CLI behavior, JSON serialization, or golden changes beyond Markdown colophon version sync. Cargo version bumped to `4.8.7`.

Review spec: [v4.8.7-fragment-apply-structured-errors-public-contract-review.md](../specifications/v4.8.7-fragment-apply-structured-errors-public-contract-review.md)

## Review conclusion

- **P0 / P1: none** — implementation matches documented additive JSON contract
- **`schema_version: 2` maintained** — additive `structured_errors[]` only
- **Legacy `errors[]` preserved** — mirror on wired paths
- **Limited exposure confirmed** — unwired paths return `errors[]` only by design
- **Client branching:** use stable `code`, not `message`

## Public contract highlights

| Topic | v4.8.7 determination |
|---|---|
| `code` | stable public identifier for branching |
| `message` | human-readable / unstable |
| `kind` / `phase` | stable enum values; tolerate unknown |
| optional fields | omit when absent — not required |
| `required_decisions[]` | separate opaque container; pair with `APPLY_REQUIRED_DECISION` |
| `kind=decision_required` | **deferred** — currently emitted as `blocking` + code |

## Provisional / deferred (P2)

- `kind=decision_required` alignment with v4.8.4 planning intent
- Extended wiring (`add_estimate` unsupported target, confirm transaction paths)
- `field_path` grammar formalization
- Registry-only codes (preview mismatch, TOCTOU, scoped write, transaction failed)

## Not in scope

- Implementation changes
- Full error path migration
- `schema_version` bump
- JSON report shape changes

## Next implementation candidates (not started)

```text
v4.8.8 — structured errors wiring expansion (optional)
Currency ISO validation (Issue #66)
Venue model (defer)
```

Alternatives: Currency ISO validation (Issue #66); Venue model (defer).
