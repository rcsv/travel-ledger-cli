# v4.6.28 — Itinerary show aggregate boundary review

## Summary

**v4.6.28** is a **documentation-only review** of the `itinerary show` aggregate boundary: the itinerary body is service-backed (`ItineraryShowServiceResult`), but human output still fetches Reservations in the CLI handler. JSON remains `ItineraryItem` only; Notes/Expenses use separate list/show services.

Review: [v4.6.28-itinerary-show-aggregate-boundary-review.md](../specifications/v4.6.28-itinerary-show-aggregate-boundary-review.md)

## Headline findings

- **Itinerary body** — service boundary complete (v4.6.10)
- **Reservations human section** — handler-side `list_reservations_for_itinerary` + `println!` (human-only aggregate)
- **JSON** — `ItineraryItem` only; no Reservations/Notes/Expenses
- **Empty reservations** — no section printed
- **Ordering** — reservations `ORDER BY id ASC`
- **Short term** — keep current split (acceptable for CLI compatibility)
- **Before GUI** — decide aggregate ownership (Options B/C/D)
- **Do not** pack Notes/Expenses into itinerary show without UX/plan — separate services already exist

## Aggregate options (summary)

| Option | Description |
|---|---|
| A | Keep handler human-only fetch (**current, recommended now**) |
| B | Add `reservations` to `ItineraryShowServiceResult` |
| C | Separate aggregate detail service |
| D | GUI-specific DTO, CLI JSON unchanged |

## Non-goals

- Implementation, JSON/human changes, aggregate DTO

## Next milestone

**v4.6.29 — itinerary show aggregate migration plan** (recommended)

Alternatives: expense write path boundary review; SQLite migration runner; v4.7.0 planning.
