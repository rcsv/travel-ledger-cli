# v4.6.42 — Reservation write service Phase R-5 adapter cleanup

## Summary

**v4.6.42** completes [v4.6.40](../specifications/v4.6.40-reservation-write-service-migration-plan.md) Phase R-5: removes the unused `print_reservation_detail(&conn, ...)` adapter after v4.6.41 write services, and closes the Reservation write migration stream.

Spec: [v4.6.42-reservation-write-service-phase-r5-adapter-cleanup.md](../specifications/v4.6.42-reservation-write-service-phase-r5-adapter-cleanup.md)

## What changed

- **Removed:** `print_reservation_detail(&conn, &Reservation)` — legacy adapter (production references zero since v4.6.41)
- **Removed:** associated `#[allow(dead_code)]`
- **Unchanged:** `print_reservation_detail_with_context`, `load_reservation_display_context`, write services, handlers

## Migration closeout

```text
Expense write migration   W-0〜W-5  — v4.6.31〜v4.6.34 完了
Note write migration      N-0〜N-5  — v4.6.35〜v4.6.38 完了
Reservation write migration R-0〜R-5 — v4.6.39〜v4.6.42 完了
```

## Compatibility

- **JSON golden:** unchanged
- **Human output:** unchanged (`reservation_cli` tests pass)
- **Markdown golden:** colophon version only

## Next milestone

v4.7.x — schema-publication / repository / GUI planning (or migration runner / FK parallel track)
