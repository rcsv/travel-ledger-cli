# v4.7.49 — P-6o update_estimate post-release review (documentation-only)

## Summary

**v4.7.49** is a **documentation-only** formal release: post-release review of P-6o **`update_estimate`** across v4.7.46–v4.7.48.

No Rust code, tests, fixtures, or CLI behavior changes. Cargo version bumped to `4.7.49`.

Review spec: [v4.7.49-p6o-update-estimate-post-release-review.md](../specifications/v4.7.49-p6o-update-estimate-post-release-review.md)

## Scope reviewed

- v4.7.46 — update_estimate planning
- v4.7.47 — update_estimate dry-run
- v4.7.48 — update_estimate --confirm

## Findings summary

- **P0:** none
- **P1:** none
- **P2:** structured errors; strict idempotency / retry token; version / ETag optimistic concurrency; GUI/API error code / field path; public Proposal Fragment schema versioning; `adoption_hints.required_decisions` container transaction re-check (field-level blocking revalidated inside confirm transaction; remaining P2 is opaque container only — not a data-safety or rollback defect)
- **P3:** test layering; CLI-unreachable preview mismatch (unit test only); numeric selector JSON number; integration test count 181

## P-6o completion

```text
planning → dry-run → confirm → review — complete
```

## v4.7 series recommendation

**Close v4.7.x at v4.7.49.** Next: **v4.8.0**.

## Next candidate

```text
v4.8.0 — delete_estimate Fragment planning（第一推薦）
```

Alternatives: Fragment apply structured errors (cross-cutting); Currency ISO validation hardening (Issue #66).

## Versioning policy note

Continue patch-stage formal releases (v4.8.0, v4.8.1, …). Do not migrate to SemVer prerelease suffix scheme (v4.8.0-1.plan, …) without workflow migration cost justification.
