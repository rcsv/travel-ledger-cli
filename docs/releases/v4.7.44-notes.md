# v4.7.44 — P-6n Planned Money post-release review (documentation-only)

## Summary

**v4.7.44** is a **documentation-only** post-release review of **P-6n Planned Money** support for Proposal Fragment `add_estimate`.

This release includes **no runtime behavior changes**.
It does **not** change Rust source code, tests, DB schema, export schema, Proposal Fragment schema, or golden data (except a version colophon update where applicable).

Reviewed scope:

```text
v4.7.41 — add_estimate planning
v4.7.42 — add_estimate dry-run
v4.7.43 — add_estimate --confirm
```

The review checks contract consistency across planning / dry-run / confirm, verifies the Estimate vs Expense semantic boundary, reviews minor-unit amount handling safety, validates target resolution (itinerary ID-first numeric selector), and confirms transaction / TOCTOU and delete_itinerary estimate-blocking behavior.

Spec: [v4.7.44-p6n-planned-money-post-release-review.md](../specifications/v4.7.44-p6n-planned-money-post-release-review.md)

## Findings

- P0: none
- P1: none
- P2: required_decisions re-check in confirm / idempotency policy / structured errors (future)
- P3: title excluded from preview agreement (intended) / currency re-validation in write helper (idempotent)

## Next candidate

```text
v4.7.45 candidate
P-6o — update_estimate planning
```

