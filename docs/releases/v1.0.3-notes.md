# v1.0.3 Release workflow validation (prerelease)

## Summary

This prerelease validates the multi-platform Release workflow. It is **not** the formal Checklist export/import release.

## Notes

- First run of `.github/workflows/release.yml`
- Linux / macOS / Windows binary assets were uploaded for workflow verification
- `Cargo.toml` was not bumped; published binaries reported `caglla --version` as `1.0.2`

## Formal release

Use [v1.0.4](https://github.com/rcsv/travel-ledger-cli/releases/tag/v1.0.4) for Checklist export/import and version-aligned release binaries.
