# v4.7.40 — P-6m itinerary ordering / move post-release review (documentation-only)

## Summary

**v4.7.40** adds a **documentation-only post-release review** of the completed structural apply operations **P-6k `reorder_itinerary`** and **P-6l `move_itinerary`**.

Reviewed scope:

```text
P-6k:
  reorder_itinerary dry-run
  reorder_itinerary --confirm

P-6l:
  move_itinerary dry-run
  move_itinerary --confirm
```

Spec: [v4.7.40-p6m-itinerary-ordering-move-post-release-review.md](../specifications/v4.7.40-p6m-itinerary-ordering-move-post-release-review.md)

## Review conclusions

- **P-6k complete** within planned same-day reorder scope
- **P-6l complete** within planned cross-day move scope
- **Design boundary consistent:**

  ```text
  reorder_itinerary = same-day ordering change
  move_itinerary    = cross-day ownership + ordering change
  ```

- **GUI backend readiness:** itinerary ID arrays plus expected-order baselines are a suitable foundation for future drag-and-drop
- **Safety:** transaction / TOCTOU revalidation and mutation boundaries are sufficiently constrained
- **P0 / P1 issues:** none found
- **Blocking cleanup:** not required before continuing Proposal Fragment work

## GUI contract guidance (documented, not implemented)

Future GUI integration should prefer **itinerary ID arrays**. Title / sort_order selector fallback remains CLI-oriented. Expected-order baselines provide stale UI detection.

## P2 / P3 follow-up (deferred)

Non-blocking P2 candidates (tests, shared reorder plan extraction, GUI contract doc, optional normalization planning) and long-term P3 items (`itinerary_items.day` deprecation evaluation, structured error codes) are **documented only** — not implemented in v4.7.40.

## What changed

- **New:** post-release review spec for P-6k / P-6l
- **Updated:** documentation indexes and current-work state
- **Not changed:** Rust code, tests, fixtures, schema v8, DB migration, runtime behavior

## Compatibility

- **JSON golden:** unchanged
- **Markdown golden:** colophon version only (`4.7.40`)
- **schema v8:** unchanged
- **DB migration:** none

## Next milestone

**v4.7.41+** — next Proposal Fragment intent, or optional P2 follow-up from the review (tests / plan extraction / normalization planning)
