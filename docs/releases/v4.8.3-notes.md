# v4.8.3 — P-6p delete_estimate post-release review (documentation-only)

## Summary

**v4.8.3** is a **documentation-only** formal release: post-release review of P-6p **`delete_estimate`** across v4.8.0–v4.8.2.

No Rust code, tests, fixtures, or CLI behavior changes. Cargo version bumped to `4.8.3`.

Review spec: [v4.8.3-p6p-delete-estimate-post-release-review.md](../specifications/v4.8.3-p6p-delete-estimate-post-release-review.md)

## Scope reviewed

- v4.8.0 — delete_estimate planning
- v4.8.1 — delete_estimate dry-run
- v4.8.2 — delete_estimate --confirm

## Findings summary

- **P0:** none
- **P1:** none
- **P2:** structured errors; strict idempotency / retry token; version / ETag optimistic concurrency; GUI/API error code / field path; public Proposal Fragment schema versioning; `adoption_hints.required_decisions` container transaction re-check (field-level blocking revalidated inside confirm transaction; remaining P2 is opaque container only — not a data-safety or rollback defect)
- **P3:** test layering; CLI-unreachable preview mismatch (unit test only); gate-after row change covered via stale `expected_*` baseline; numeric selector JSON number; integration test count 26; existing CLI `estimate delete` remains unscoped by design; rollback vs undo wording distinct

## P-6p completion

```text
planning → dry-run → confirm → review — complete
```

Planned Money Fragment CRUD (add / update / delete) is complete via P-6n / P-6o / P-6p.

## Next candidate

```text
Fragment apply structured errors / API readiness（cross-cutting — first recommendation）
```

Alternatives: Currency ISO validation hardening (Issue #66); Venue model implementation (defer).

## Versioning policy note

Continue patch-stage formal releases (v4.8.0, v4.8.1, v4.8.2, v4.8.3, …). Do not migrate to SemVer prerelease suffix scheme without workflow migration cost justification.
