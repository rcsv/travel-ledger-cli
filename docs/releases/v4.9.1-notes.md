# v4.9.1 — Trip optional metadata DB implementation

## Summary

**v4.9.1** implements Trip optional metadata in the database and CLI:

- `trips.main_destination` / `main_destination_country_code` / `default_currency`（nullable）
- `trip add` / `trip update` / `trip show` / JSON output
- ISO 3166-1 alpha-2 strict country validation（2026-07 static snapshot）
- `default_currency` uses existing IsoStrict currency validation

Cargo version bumped to `4.9.1`. Markdown colophon synced. Export/import schema unchanged. Expense auto-apply unchanged.

Implementation spec: [v4.9.1-trip-optional-metadata-db-implementation.md](../specifications/v4.9.1-trip-optional-metadata-db-implementation.md)

## Trip metadata (high level)

| Field | Role |
|---|---|
| `main_destination` | Representative destination label（not Venue） |
| `main_destination_country_code` | ISO 3166-1 alpha-2 helper（not currency） |
| `default_currency` | Input suggestion only（not Expense currency ownership） |

All fields optional; existing Trips remain valid without them. CLI JSON via `TripCliJson`; export JSON omits metadata.

## Compatibility

- Existing DB upgraded via nullable migration（metadata NULL）
- export/import / validate-export / Proposal: unchanged
- `trip list` plain text: unchanged

## Next candidates

1. **v4.9.2** — Desktop readiness service facade
2. Desktop vertical slice（read-only first）

## Prior release

**[v4.9.0](v4.9.0-notes.md)** — Desktop transition and Trip metadata foundation (released, documentation-only)
