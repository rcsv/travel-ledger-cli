# v4.7.6 — Public JSON examples and concept stream post-review

[v4.7.5](v4.7.5-public-examples-ai-json-generation-guide.md) の narrative examples を踏まえ、Travel Ledger を **「読めば分かる」から「見れば分かる」** へ進める public JSON example files と v4.7.x concept stream の post-review です。

| Item | Status |
|---|---|
| Release | v4.7.6 |
| Scope | public JSON examples + concept stream post-review |
| 実装 | **なし** |

関連:

| Document | 内容 |
|---|---|
| [public/examples/](../public/examples/) | schema v8 Trip JSON（validate-export 対象） |
| [public/examples-non-normative/](../public/examples-non-normative/) | Envelope / Fragment 概念例 |
| [v4.7.0〜v4.7.5 specs](v4.7.2-trip-proposal-envelope-concept-spec.md) | concept stream |
| [export-schema.md](export-schema.md) | schema v8 正本（実装者向け） |

---

## Status（結論サマリ）

| 観点 | 判定 |
|---|---|
| schema v8 public JSON | **3 files** — validate-export 通過 |
| Proposal / Fragment JSON | **non-normative** — schema 未確定 |
| validate-export 境界 | examples README で明確化 |
| concept stream post-review | 整合 — 軽微な follow-up を記録 |
| 実装 | **なし** |

---

## 1. 目的

v4.7.0〜v4.7.5 で整理した public schema / proposal / AI authoring 入口に、**実際の JSON ファイル** を追加する。

```text
docs/public/examples/              — 採用済み schema v8 Trip
docs/public/examples-non-normative/ — 候補案（Envelope / Fragment）
```

家系図ソフトのサンプル図と同様、**見れば schema の意図が伝わる** 状態を目指す。

---

## 2. schema v8 public JSON examples

| ファイル | 用途 |
|---|---|
| `schema-v8-minimal-trip.json` | 最小構成 |
| `schema-v8-okinawa-sesoko-trip.json` | canonical 短縮版 |
| `schema-v8-with-reservations-expenses-notes.json` | nested reservations / expenses / notes |

### 2.1 性質

```text
採用済み Trip の例
候補案ではない
validate-export 対象
JSON golden ではない（public docs assets）
```

### 2.2 validate-export

v4.7.6 時点で 3 ファイルすべて `trip validate-export` **PASS**。

`trip.id` / `created_at` / `updated_at` は deserialize 互換のため example に含む（[examples/README.md](../public/examples/README.md) 参照）。

---

## 3. Non-normative Proposal / Fragment examples

| ファイル | 用途 |
|---|---|
| `trip-proposal-envelope.example.json` | 全体未採用案 |
| `proposal-fragment.example.json` | 部分提案 |
| `materialize-before-after.md` | gate 前後 |

```text
_disclaimer フィールドで non-normative を明示
JSON schema 未確定
validate-export 対象外
```

---

## 4. validate-export narrative（再掲）

| JSON | validate-export |
|---|---|
| schema v8 Trip（`examples/`） | **対象** |
| Trip Proposal Envelope | 対象外 |
| Proposal Fragment | 対象外 |
| materialize / apply 後 | schema v8 として **対象** |

---

## 5. Concept stream post-review（v4.7.0〜v4.7.5）

実例目線での軽量レビュー。問題の全面修正は scope 外 — concerns / follow-up の記録。

### 5.1 schema v8 Trip examples と public docs

| 観点 | 判定 | メモ |
|---|---|---|
| 三層モデル（Trip / Envelope / Fragment） | **整合** | ディレクトリ分離で強化 |
| nested days / itineraries | **整合** | minimal + okinawa + rich で段階的 |
| validate-export 説明 | **整合** | examples README + materialize doc |

**Follow-up candidate:** [export-schema.md](export-schema.md) の schema version 表に v7/v8 の記述追従（実装者向け doc drift — v4.7.6 scope 外）

### 5.2 Trip Proposal Envelope と schema v8 Trip の境界

| 観点 | 判定 | メモ |
|---|---|---|
| 日付未定 → Envelope | **整合** | envelope example に `date: null` |
| 採用済み → schema v8 | **整合** | examples/ のみ validate-export |
| 混在防止 | **整合** | `_disclaimer` + README |

**Concern（既知）:** v4.7.5 [examples.md](../public/examples.md) の inline 最小 JSON は trip メタデータ省略 — v4.7.6 で file 参照を primary に更新

### 5.3 Proposal Fragment ≠ 小さい Trip

| 観点 | 判定 | メモ |
|---|---|---|
| target 参照 | **整合** | fragment example に `trip_reference` |
| intent / adoption_hints | **整合** | v4.7.3 spec と一致 |
| 誤解防止 | **整合** | `_disclaimer` に明示 |

### 5.4 materialize / apply gate と examples

| 観点 | 判定 | メモ |
|---|---|---|
| gate 前後フロー | **整合** | materialize-before-after.md |
| blocking / warnings | **整合** | envelope / fragment hints |

### 5.5 AI JSON generation guide と examples

| 観点 | 判定 | メモ |
|---|---|---|
| 捏造禁止 | **整合** | fragment notes / missing_fields |
| missing_fields | **整合** | envelope example |
| validate-export 境界 | **整合** | ai guide + examples README |

### 5.6 validate-export と golden

| 観点 | 判定 | メモ |
|---|---|---|
| public examples PASS | **確認済** | 3 schema v8 files |
| JSON golden 不変 | **維持** | samples/okinawa 変更なし |
| golden vs validate-export | **既知ギャップ** | canonical golden は trip.id 省略 — regression 比較用。public examples は validate-export 互換を優先 |

---

## 6. Concerns / follow-up candidates

| # | 項目 | 優先度 | 備考 |
|---|---|---|---|
| 1 | export-schema.md v7/v8 表記追従 | 低 | 実装者 doc |
| 2 | validate-export と export 出力の trip メタデータ差 | 低 | 将来 doc または tooling 整理 |
| 3 | public JSON example の CI validate-export チェック | 中 | 将来 — v4.7.6 では手動確認 |
| 4 | Proposal JSON schema 確定 | 未着手 | 別マイルストーン |

---

## 7. Non-goals（v4.7.6）

```text
src/ 実装
schema v8 変更
Proposal / Fragment JSON schema 確定
import / materialize / apply 実装
DB migration
GUI
migration runner / FK hardening
```

---

## 8. v4.7.x 位置づけ

| Version | 内容 |
|---|---|
| v4.7.0 | public direction |
| v4.7.1 | public docs outline |
| v4.7.2 | Trip Proposal Envelope |
| v4.7.3 | Proposal Fragment |
| v4.7.4 | materialize gate |
| v4.7.5 | public examples / AI guide（narrative） |
| v4.7.6 | **public JSON examples + post-review（本書）** |

v4.7.x concept + authoring + **見える JSON** が揃った。

---

## Next milestone

**未確定** — Proposal implementation planning、export-schema v8 表記追従、public examples CI チェック（相談）
