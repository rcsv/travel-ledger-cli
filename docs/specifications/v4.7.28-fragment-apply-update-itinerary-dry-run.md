# v4.7.28 — Fragment apply update_itinerary dry-run (P-6i)

[v4.7.27](v4.7.27-fragment-apply-add-reservation-confirm.md) の P-6h 完了後、**P-6i** として Proposal Fragment の **`update_itinerary` dry-run** を実装します。

| Item | Status |
|---|---|
| Release | v4.7.28 |
| Scope | `fragment apply <file> --dry-run --trip <id>` — `update_itinerary` preview のみ |
| Phase | **P-6i Phase A** |
| Plan | [v4.7.28-p6i-update-itinerary-implementation-plan.md](v4.7.28-p6i-update-itinerary-implementation-plan.md) |

---

## Status（結論サマリ）

| 観点 | v4.7.28 判定 |
|---|---|
| Command | **`fragment apply <file> --dry-run --trip <id>`** — `update_itinerary` preview |
| intent | **`update_itinerary` のみ**（dry-run） |
| target | **itinerary のみ** |
| DB | **read-only** — itinerary UPDATE なし |
| `--confirm` | **未対応**（v4.7.29 候補） |

---

## 1. supported scope

- in-place 属性: `title`, `note`, `location`, `category`, `start_time`/`time`, `duration_minutes`/`duration`, `travel_minutes`/`travel_time_minutes`/`travel`
- gate report: `preview.itinerary_field_changes[]` — `{ field, before, after }`
- preview JSON: patched itinerary 状態
- `trip validate-export` PASS

## 2. blocking

- trip / day target
- unresolved / ambiguous target
- `required_decisions` 非空（`adoption_hints.conflicts` 含む merge 後）
- `expected_<field>` baseline mismatch
- no-op / invalid field / empty candidate

## 3. Non-goals（v4.7.28）

- `--confirm` / DB UPDATE
- day 移動 / `sort_order` / reorder / delete
- child entity 更新
- schema v8 変更 / DB migration

## 4. Next milestone

**v4.7.29** — P-6i `update_itinerary --confirm`
