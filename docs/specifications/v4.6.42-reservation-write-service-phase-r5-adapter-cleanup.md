# v4.6.42 — Reservation write service Phase R-5 adapter cleanup

[v4.6.41](v4.6.41-reservation-write-service-phase-r2-r3.md) Phase R-2+R-3 実装後に残置されていた `print_reservation_detail(&conn, ...)` adapter を削除し、**Reservation write migration** を closeout する。

| Item | Status |
|---|---|
| Release | v4.6.42 |
| Scope | adapter 削除 + migration closeout |
| Plan reference | v4.6.40 Phase R-5 |

関連:

| Version | Phase | 内容 |
|---|---|---|
| v4.6.39 | R-0 | write path boundary review |
| v4.6.40 | R-1 | write service migration plan |
| v4.6.41 | R-2+R-3 | write services + handler 接続 |
| **本書** | **R-5** | **adapter cleanup — migration 完了** |

---

## Status

| 観点 | 判定 |
|---|---|
| `print_reservation_detail(&conn, ...)` production 参照 | **v4.6.41 でゼロ** — 本 release で **削除** |
| `print_reservation_detail_with_context` | **正式 human 経路** — read show + write add/update |
| `load_reservation_display_context` | **維持** — show service + write services 共用 |
| write services | **変更なし** — `reservation_add` / `update` / `delete` |
| handler 責務 | **維持** — Params / service / println! / human |
| JSON golden | **不変** |
| human output | **不変**（`reservation_cli` tests） |
| Reservation write migration | **完了** |

**結論（確定）:**

```text
Reservation write path は thin service layer 経由に整理済み。
write add/update は print_reservation_detail_with_context + service 返却 context が正式経路。
print_reservation_detail(&conn, ...) は production 参照ゼロのため削除。
Expense / Note / Reservation の write path 整理ストリームが一通り揃った。
```

---

## 1. v4.6.41 後の write path 確認

### handler（`main.rs`）

| Action | Service | handler 残責務 |
|---|---|---|
| **Add** | `reservation_add` | `ReservationAddParams` → `println!` → `_with_context` |
| **Update** | `reservation_update` | `ReservationUpdateParams`（clear raw）→ `println!` → `_with_context` |
| **Delete** | `reservation_delete` | `ReservationDeleteParams` → `println!` → provider 行 |

handler から **除去済み（v4.6.41）:**

```text
clear フラグ解釈
add_reservation / update_reservation / delete_reservation
get_reservation
print_reservation_detail(&conn, ...)
```

### read path（変更なし）

| Action | Service | Human / JSON |
|---|---|---|
| **List** | `reservation_list` | `ReservationListJson` / `print_reservation_list` |
| **Show** | `reservation_show` | `Reservation` JSON / `_with_context` |

---

## 2. adapter 棚卸し

### 削除（v4.6.42）

| Helper | 理由 |
|---|---|
| `print_reservation_detail(&conn, &Reservation)` | write から外れ（v4.6.41）、production / tests 参照ゼロ。`_with_context` + service context が正式経路 |

### 正式経路（削除対象外）

| Helper | 用途 |
|---|---|
| `load_reservation_display_context` | show + write services の context 解決 |
| `print_reservation_detail_with_context` | show / add / update human |
| `print_reservation_list` | list human |
| `add_reservation` / `update_reservation` / `delete_reservation` | write services から呼び出し（mutation 本体） |

---

## 3. Expense / Note / Reservation write migration 比較

| Entity | N/W/R-5 | cleanup 種別 |
|---|---|---|
| Expense | W-5 (v4.6.34) | 実装 — 3 adapter 削除 |
| Note | N-5 (v4.6.38) | docs-only — adapter なし |
| **Reservation** | **R-5 (v4.6.42)** | **実装 — 1 adapter 削除** |

---

## 4. Files changed

| ファイル | 変更 |
|---|---|
| `src/reservation.rs` | `print_reservation_detail(&conn, ...)` 削除 |

**未変更:** write services, `main.rs`, JSON golden, schema。

---

## 5. Merge gate（確認済み）

```text
□ grep: print_reservation_detail production 参照ゼロ
□ cargo test --test reservation_cli — PASS
□ make check — PASS
□ JSON golden — 変更なし
□ Okinawa colophon — Version のみ
```

---

## Next candidates

| 候補 | 内容 |
|---|---|
| parallel track | migration runner / FK hardening |
| v4.7.x | repository 層 / schema-publication |
| R-H | `RETURNING id` hardening（任意） |
