{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window — read-only Travel Ledger desktop slice",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "dialog:default"
  ]
}
