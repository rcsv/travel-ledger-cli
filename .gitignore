/target
caglla.db

# ルート直下の試験用 JSON（trip export などの一時ファイル）
# 先頭 / でサブディレクトリは対象外。一般的な設定ファイルは除外する。
/*.json
!package.json
!package-lock.json
!tsconfig.json
!jsconfig.json
!components.json
!.eslintrc.json
!vercel.json

# macOS
.DS_Store
.AppleDouble
.LSOverride
._*
.Spotlight-V100
.Trashes
.fseventsd
.TemporaryItems

# Windows
Thumbs.db
Thumbs.db:encryptable
ehthumbs.db
ehthumbs_vista.db
Desktop.ini
$RECYCLE.BIN/
*.stackdump

# Linux
*~
.directory
.Trash-*
.nfs*
